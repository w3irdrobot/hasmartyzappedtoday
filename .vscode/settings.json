{
    "sqltools.connections": [
        {
            "previewLimit": 50,
            "driver": "SQLite",
            "name": "local zaps",
            "database": "${workspaceFolder:hasmartyzappedtoday}/zaps.db"
        }
    ]
}
